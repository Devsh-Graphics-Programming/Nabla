#version 430 core

layout(std140, binding = 0) uniform InstanceData
{
	struct {
		mat4 m4;
		mat3 m3;
		vec3 v3;
		float f;
	} data[10];
} instData;

out vec4 vcolor;

#define ACCESS instData.data[gl_InstanceID]
void main()
{
	
	gl_Position = vcolor = ACCESS.m4 * vec4(ACCESS.f) + (ACCESS.m3 * ACCESS.v3).xxxx;
}