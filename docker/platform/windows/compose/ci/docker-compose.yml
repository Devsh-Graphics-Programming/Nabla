version: '3'

services:
  base:
    build:
      context: ../
      args:
      - THIS_PROJECT_WORKING_DIRECTORY=${THIS_PROJECT_WORKING_DIRECTORY}
      - THIS_PROJECT_NABLA_DIRECTORY=${THIS_PROJECT_NABLA_DIRECTORY}
      - NABLA_TARGET_REVISION=${NABLA_TARGET_REVISION}
    environment:
    - THIS_PROJECT_WORKING_DIRECTORY=${THIS_PROJECT_WORKING_DIRECTORY}
    - THIS_PROJECT_NABLA_DIRECTORY=${THIS_PROJECT_NABLA_DIRECTORY}
    - NABLA_TARGET_REVISION=${NABLA_TARGET_REVISION}
    image: artifactory.devsh.eu/nabla/windows/base:latest
    container_name: dev.nabla.base.x86_64.windows
    networks:
      - docker_default
    volumes:
      - type: volume
        source: ssh
        target: ${THIS_PROJECT_SSH_DIRECTORY}
    entrypoint: ["%THIS_PROJECT_NABLA_DIRECTORY%/docker/platform/windows/scripts/build.bat", "--cishallowconfigure", "%NABLA_TARGET_REVISION%"]
networks:
  docker_default:
    external: true
volumes:
    ssh:
      external: true